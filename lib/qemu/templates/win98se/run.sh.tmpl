#!/bin/bash

CUR_DIR=$(cd `dirname $0` && pwd)
cd $CUR_DIR

qemu-system-x86_64 \
  ${mounts} \
  -enable-kvm \
  -cpu "${cpu}" \
  -m ${memory} \
  -display ${display} \
  -monitor vc \
  -audiodev pa,id=pa1 \
  -device ${audiodev},audiodev=pa1 \
  -usbdevice tablet \
  -device VGA
